# 📖 Как активировать виртуальное окружение в DRL

## 🚀 Быстрый способ

### Для PowerShell:
```powershell
cd ai\DRL
.\venv\Scripts\Activate.ps1
```

Или используйте готовый скрипт:
```powershell
.\ai\DRL\activate_venv.ps1
```

### Для CMD (командная строка Windows):
```cmd
cd ai\DRL
venv\Scripts\activate.bat
```

## ⚠️ Если возникает ошибка политики выполнения в PowerShell

Если при попытке активации вы видите ошибку типа:
```
Activate.ps1 cannot be loaded because running scripts is disabled on this system
```

**Решение:**

1. **Временно для текущей сессии (рекомендуется):**
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
   .\venv\Scripts\Activate.ps1
   ```

2. **Или используйте CMD активатор:**
   ```powershell
   venv\Scripts\activate.bat
   ```

## ✅ Как проверить, что окружение активировано?

### Способ 1: Визуальная проверка
После активации в начале строки PowerShell должно появиться `(venv)`:
```
(venv) PS C:\Users\...\ai\DRL>
```

### Способ 2: Проверка переменной окружения
```powershell
$env:VIRTUAL_ENV
```
Если окружение активно, вы увидите путь к venv.

### Способ 3: Проверка пути к Python
```powershell
python -c "import sys; print(sys.executable)"
```
Если путь содержит `venv`, окружение активно.

## 🔄 Деактивация окружения

Чтобы деактивировать окружение, просто выполните:
```powershell
deactivate
```

## 📝 Пример полной последовательности действий

```powershell
# 1. Перейти в директорию проекта
cd C:\Users\Макар\PycharmProjects\trading\CryptoTrade

# 2. Перейти в DRL
cd ai\DRL

# 3. Активировать окружение
.\venv\Scripts\Activate.ps1

# 4. Проверить активацию
$env:VIRTUAL_ENV
python --version

# 5. Работать с проектом
python your_script.py

# 6. Деактивировать (когда закончите)
deactivate
```

## 💡 Полезные команды

После активации окружения вы можете:
- Установить пакеты: `pip install package_name`
- Посмотреть установленные пакеты: `pip list`
- Обновить pip: `python -m pip install --upgrade pip`
- Установить зависимости: `pip install -r requirements.txt`

## 🎯 Альтернативный способ (без активации)

Вы также можете использовать Python из виртуального окружения напрямую без активации:
```powershell
.\ai\DRL\venv\Scripts\python.exe your_script.py
```

Но активация удобнее, так как позволяет просто использовать команды `python` и `pip` без указания полного пути.

