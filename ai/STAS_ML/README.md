# 🚀 Улучшенная DRL Система для Торговли Криптовалютами

## 📋 Обзор

Эта система предоставляет полнофункциональную среду для обучения и тестирования агентов глубокого обучения с подкреплением (DRL) для торговли криптовалютами с начальным балансом **$10,000**.

## 🎯 Ключевые улучшения

- ✅ **Начальный баланс $10,000** вместо $100
- ✅ **Комплексная система метрик** с отслеживанием:
  - Итоговый баланс в $ и %
  - Максимальная и средняя просадка
  - Коэффициент Шарпа, Кальмара, Сортино
  - Фактор прибыли и коэффициент восстановления
  - Детальная статистика по сделкам
- ✅ **Улучшенная отчетность** с красивыми графиками
- ✅ **Продолжение обучения** существующих моделей
- ✅ **Реалистичное моделирование** с комиссиями, проскальзыванием и спредом

## 🗂️ Структура проекта

```
CryptoTrade/ai/DRL/
├── agents/                 # DRL агенты (PPO, DQN)
├── config/                 # Конфигурации
├── environment/            # Торговая среда
├── training/              # Модули обучения
├── evaluation/            # Оценка моделей
├── utils/                 # Утилиты и метрики
├── mvp_train.py          # Главный скрипт для обучения
└── README.md             # Это руководство
```

## 🚀 Быстрый старт

### 1. Автоматическое обучение (рекомендуется)

```bash
cd CryptoTrade/ai/STAS_ML
python mvp_train.py
```

Это автоматически:
- Использует BTCUSDT на дневном таймфрейме
- Запускает PPO агента с оптимизированной схемой наград
- Начинает с $10,000
- Обучает 1,000,000 шагов (~3-4 часа)
- Продолжает обучение существующей модели если она есть

### 2. Быстрое обучение с параметрами

```bash
# Быстрое обучение ETHUSDT
python mvp_train.py --quick --symbol ETHUSDT --timesteps 200000

# Обучение DQN агента
python mvp_train.py --quick --agent DQN --timesteps 500000
```

### 3. Интерактивное обучение

```bash
python mvp_train.py --help  # Показать все опции
```

## 📊 Создание и обучение моделей

### Создание конфигурации

```python
from CryptoTrade.ai.STAS_ML.config.trading_config import TradingConfig

# Создание базовой конфигурации
config = TradingConfig(
    symbol='BTCUSDT',
    timeframe='1d',
    initial_balance=10000.0,  # $10k начальный баланс
    reward_scheme='optimized'
)

# Кастомная конфигурация
config = TradingConfig(
    exchange='binance',
    symbol='ETHUSDT',
    timeframe='4h',
    initial_balance=10000.0,
    commission_rate=0.001,  # 0.1% комиссия
    slippage_rate=0.0005,  # 0.05% проскальзывание
    spread_rate=0.0002,  # 0.02% спред
    reward_scheme='aggressive'
)
```

### Обучение модели

```python
from CryptoTrade.ai.STAS_ML.training.train import DRLTrainer

# Создание тренера
trainer = DRLTrainer(config, resume_training=True)

# Обучение PPO агента
agent = trainer.train(
    total_timesteps=1000000,
    agent_type="PPO"
)

# Обучение DQN агента  
agent = trainer.train(
    total_timesteps=500000,
    agent_type="DQN"
)
```

### Быстрое обучение

```python
from CryptoTrade.ai.STAS_ML.training.train import quick_train

# Один вызов для полного обучения
agent = quick_train(
    symbol="BTCUSDT",
    timeframe="1d",
    agent_type="PPO",
    timesteps=1000000,
    reward_scheme="optimized"
)
```

## 📈 Сохранение и загрузка моделей

### Автоматическое сохранение

Модели автоматически сохраняются в:
```
models/
├── BTCUSDT_1d_optimized/
│   ├── final_model.zip          # Финальная модель
│   ├── best_model.zip           # Лучшая модель
│   └── checkpoints/             # Промежуточные сохранения
│       ├── model_50000_steps.zip
│       ├── model_100000_steps.zip
│       └── ...
└── logs/
    └── BTCUSDT_1d_optimized/    # Логи TensorBoard
```

### Продолжение обучения

Система автоматически найдет и продолжит обучение существующей модели:

```python
# Автоматически найдет и продолжит обучение
trainer = DRLTrainer(config, resume_training=True)
agent = trainer.train(total_timesteps=500000, agent_type="PPO")
```

### Ручная загрузка

```python
from CryptoTrade.ai.STAS_ML.agents.ppo_agent import PPOAgent

agent = PPOAgent(config)
agent.load("models/BTCUSDT_1d_optimized/final_model.zip", env)
```

## 🧪 Оценка и тестирование моделей

### Быстрая оценка

```bash
cd CryptoTrade/ai/STAS_ML
python evaluation/evaluate.py models/BTCUSDT_1d_optimized/final_model.zip --episodes 10
```

### Программная оценка

```python
from CryptoTrade.ai.STAS_ML.evaluation.evaluate import quick_evaluate

# Оценка модели
evaluator, results, report = quick_evaluate(
    model_path="models/BTCUSDT_1d_optimized/final_model.zip",
    symbol="BTCUSDT",
    timeframe="1d",
    agent_type="PPO",
    episodes=10
)

# Показать итоговые метрики
print(f"Средняя доходность: {results['mean_return_pct']:.1f}%")
print(f"Максимальная просадка: {results['mean_drawdown_pct']:.1f}%")
print(f"Винрейт: {results['mean_win_rate_pct']:.1f}%")
```

### Детальная оценка

```python
from CryptoTrade.ai.STAS_ML.evaluation.evaluate import DRLEvaluator
from CryptoTrade.ai.STAS_ML.environment.trading_env import TradingEnv

# Создание оценщика
config = TradingConfig(symbol="BTCUSDT", timeframe="1d", initial_balance=10000.0)
evaluator = DRLEvaluator("models/BTCUSDT_1d_optimized/final_model.zip", config, "PPO")

# Создание тестовой среды
env = TradingEnv(config)

# Оценка на 20 эпизодах
results = evaluator.evaluate_episodes(env, num_episodes=20)

# Создание отчета
report = evaluator.create_detailed_report(save_path="evaluation_results")

# Визуализация
evaluator.plot_results(save_path="evaluation_results")

# Сравнение с базовой стратегией
comparison = evaluator.compare_with_baseline("buy_hold")
```

## 📊 Понимание метрик

### Финансовые метрики

- **Итоговый баланс**: Конечная сумма в долларах
- **Общая доходность %**: Процентная доходность от начального капитала
- **Общая доходность $**: Прибыль/убыток в долларах
- **Максимальная просадка %**: Наибольшее падение от пика
- **Максимальная просадка $**: Просадка в долларах

### Торговые метрики

- **Винрейт**: Процент прибыльных сделок
- **Фактор прибыли**: Отношение общей прибыли к общему убытку
- **Среднее количество сделок**: Торговая активность агента
- **Последовательные победы/поражения**: Анализ серий

### Коэффициенты риска

- **Коэффициент Шарпа**: Доходность с поправкой на риск
- **Коэффициент Кальмара**: Годовая доходность / Максимальная просадка
- **Коэффициент Сортино**: Как Шарпа, но только отрицательная волатильность
- **Коэффициент восстановления**: Чистая прибыль / Максимальная просадка

## ⚙️ Конфигурация схем наград

### Доступные схемы

1. **default**: Сбалансированная схема
2. **conservative**: Акцент на стабильность и малые просадки
3. **aggressive**: Максимизация прибыли
4. **optimized**: Оптимальная схема (винрейт >60%, просадка <20%)

### Кастомная схема наград

```python
config = TradingConfig(
    symbol='BTCUSDT',
    timeframe='1d',
    initial_balance=10000.0,
    reward_scheme='custom',
    custom_reward_weights={
        'profit': 1.0,
        'drawdown': -2.0,
        'sharpe': 0.5,
        'trade_quality': 0.3,
        'volatility': -0.2,
        'consistency': 0.4
    }
)
```

## 🔍 Мониторинг обучения

### TensorBoard

```bash
# Запуск TensorBoard для мониторинга
tensorboard --logdir logs

# Открыть http://localhost:6006 в браузере
```

### Логи

Логи сохраняются в:
```
logs/
├── BTCUSDT_1d_optimized/
│   ├── train/               # Логи обучения
│   ├── val/                 # Логи валидации
│   ├── config.json          # Конфигурация эксперимента
│   └── tensorboard/         # TensorBoard логи
```

## 🛠️ Расширенное использование

### Множественное обучение

```python
from CryptoTrade.ai.STAS_ML.config.trading_config import get_popular_configs

# Получить популярные конфигурации
configs = get_popular_configs()

# Обучить несколько моделей
for config in configs[:3]:  # Первые 3 конфигурации
    trainer = DRLTrainer(config, resume_training=True)
    agent = trainer.train(total_timesteps=500000, agent_type="PPO")
```

### Сравнение стратегий

```python
from CryptoTrade.ai.STAS_ML.utils.metrics import compare_strategies

# Оценить несколько моделей
model_paths = [
    "models/BTCUSDT_1d_optimized/final_model.zip",
    "models/ETHUSDT_1d_optimized/final_model.zip",
    "models/BNBUSDT_4h_aggressive/final_model.zip"
]

metrics_list = []
for model_path in model_paths:
    evaluator, results, report = quick_evaluate(model_path, episodes=10)
    metrics_list.append(results['comprehensive_metrics'])

# Создать таблицу сравнения
comparison_df = compare_strategies(
    metrics_list,
    ['BTC Daily', 'ETH Daily', 'BNB 4H']
)
print(comparison_df)
```

## 📚 Примеры использования

### Пример 1: Простое обучение

```python
# Быстрое обучение BTCUSDT
from CryptoTrade.ai.STAS_ML.training.train import quick_train

agent = quick_train(
    symbol="BTCUSDT",
    timeframe="1d",
    agent_type="PPO",
    timesteps=1000000
)
```

### Пример 2: Продвинутая конфигурация

```python
from CryptoTrade.ai.STAS_ML.config.trading_config import TradingConfig
from CryptoTrade.ai.STAS_ML.training.train import DRLTrainer

# Кастомная конфигурация
config = TradingConfig(
    symbol='ETHUSDT',
    timeframe='4h',
    initial_balance=10000.0,
    commission_rate=0.0015,  # Более высокая комиссия
    reward_scheme='conservative',
    lookback_window=100  # Больше исторических данных
)

# Обучение с кастомными параметрами
trainer = DRLTrainer(config, resume_training=True)
agent = trainer.train(
    total_timesteps=2000000,
    agent_type="PPO",
    eval_freq=20000,
    save_freq=50000
)
```

### Пример 3: Полная оценка

```python
from CryptoTrade.ai.STAS_ML.evaluation.evaluate import DRLEvaluator
from CryptoTrade.ai.STAS_ML.environment.trading_env import TradingEnv
from CryptoTrade.ai.STAS_ML.config.trading_config import TradingConfig

# Оценка модели
config = TradingConfig(symbol="BTCUSDT", timeframe="1d", initial_balance=10000.0)
evaluator = DRLEvaluator("models/BTCUSDT_1d_optimized/final_model.zip", config, "PPO")

env = TradingEnv(config)
results = evaluator.evaluate_episodes(env, num_episodes=50)

# Создание полного отчета
report = evaluator.create_detailed_report(save_path="detailed_evaluation")
evaluator.plot_results(save_path="detailed_evaluation")

# Сравнение с базовой стратегией
comparison = evaluator.compare_with_baseline("buy_hold")

print(f"Агент доходность: {results['mean_return_pct']:.1f}%")
print(f"Buy&Hold доходность: {comparison['baseline_return']:.1f}%")
print(f"Превышение: {comparison['outperformance']:.1f}%")
```

## 🎯 Лучшие практики

### Обучение
1. **Начните с малого**: 100k шагов для тестирования, затем 1M+ для серьезного обучения
2. **Используйте продолжение**: `resume_training=True` для улучшения существующих моделей  
3. **Мониторьте TensorBoard**: Отслеживайте прогресс в реальном времени
4. **Сохраняйте checkpoints**: Регулярные сохранения каждые 20-50k шагов

### Оценка
1. **Множественные эпизоды**: Используйте 10-50 эпизодов для надежной оценки
2. **Сравнивайте с базовой**: Всегда сравнивайте с buy&hold стратегией
3. **Анализируйте метрики**: Не только доходность, но и просадки, винрейт
4. **Визуализируйте**: Используйте графики для понимания поведения агента

### Оптимизация
1. **Экспериментируйте со схемами наград**: optimized → aggressive → custom
2. **Попробуйте разные таймфреймы**: 1d для трендов, 4h для активной торговли
3. **Настройте lookback_window**: 50-200 в зависимости от стратегии
4. **Используйте ансамбли**: Комбинируйте несколько моделей

## 🐛 Устранение неполадок

### Частые проблемы

1. **Данные не найдены**
   ```
   FileNotFoundError: Данные не найдены для BTCUSDT
   ```
   - Убедитесь что данные находятся в `CryptoTrade/data/binance/BTCUSDT/1d/`

2. **Ошибки импорта**
   ```
   ImportError: No module named 'stable_baselines3'
   ```
   - Установите: `pip install stable-baselines3 torch tensorboard`

3. **Модель не найдена**
   ```
   Модель не найдена для продолжения обучения
   ```
   - Проверьте путь в `models/` или установите `resume_training=False`

4. **Недостаточно памяти**
   - Уменьшите `lookback_window` в конфигурации
   - Используйте меньше `total_timesteps`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь что все зависимости установлены
3. Проверьте наличие данных в правильном формате
4. Используйте режим отладки с меньшим количеством шагов

---

## 🎉 Заключение

Эта улучшенная система DRL предоставляет полный цикл для:
- ✅ Обучения агентов с $10k стартовым капиталом
- ✅ Комплексной оценки с детальными метриками
- ✅ Профессиональной отчетности
- ✅ Продолжения обучения существующих моделей
- ✅ Реалистичного моделирования торговых условий

Начните с `python mvp_train.py` и экспериментируйте! 🚀