# Binance Browser Automation Bot

🤖 Автоматизований браузерний бот для роботи з Binance, інтегрований з CryptoTrade Python проектом

## 🌟 Особливості

- **Stealth режим**: Маскування автоматизації від систем виявлення Binance
- **Ротація проксі**: Автоматична зміна IP для уникнення блокувань
- **Людська поведінка**: Імітація реальних дій користувача
- **Python інтеграція**: Повна сумісність з існуючим CryptoTrade проектом
- **Cloud-ready**: Готовий для розгортання на хмарних платформах
- **Моніторинг**: Детальне логування та сповіщення

## 📁 Розташування в проекті

```
CryptoTrade/
├── ExchangeСonnection/
│   └── Binance/
│       ├── binance_signal.py          # Основний Python код
│       └── browser-automation/         # Браузерна автоматизація
│           ├── bot.js                 # Основний браузерний бот
│           ├── start.js               # Інтерактивний запуск
│           ├── package.json           # Node.js залежності
│           ├── .env.example          # Приклад конфігурації
│           └── README.md             # Ця документація
```

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
# Перейти до папки браузерної автоматизації
cd CryptoTrade/ExchangeСonnection/Binance/browser-automation

# Встановити Node.js залежності
npm install

# Встановити Playwright браузери
npx playwright install chromium
```

### 2. Налаштування

```bash
# Створити файл конфігурації
cp .env.example .env

# Відредагувати налаштування
notepad .env  # Windows
# або
nano .env     # Linux/macOS
```

### 3. Мінімальна конфігурація (.env)

```env
# Проксі (ОБОВ'ЯЗКОВО для production)
PROXY_1=http://username:password@proxy1.example.com:8080
PROXY_2=http://username:password@proxy2.example.com:8080

# Binance облікові дані
BINANCE_EMAIL=your_email@example.com
BINANCE_PASSWORD=your_secure_password

# Торгові налаштування
FROM_CURRENCY=USDT
TO_CURRENCY=BTC
TRADE_AMOUNT=10  # Почніть з малої суми!

# Безпека
HEADLESS=true
SCHEDULED=false
```

### 4. Запуск

```bash
# Інтерактивний режим
node start.js

# Або прямий запуск
npm start
```

## 🔗 Інтеграція з Python проектом

### Автоматична інтеграція

Браузерний бот автоматично:
- 🔍 Шукає конфігурацію Python проекту
- 💾 Зберігає результати в `data/last_execution.json`
- 📝 Використовує спільні логи
- ⚙️ Синхронізує налаштування

### Мостова конфігурація

Після запуску пункту "6. Інтеграція з Python сигналами" створюється:

```json
// python_integration.json
{
  "pythonSignalPath": "../binance_signal.py",
  "browserBotPath": "./",
  "integration": {
    "enabled": true,
    "syncData": true,
    "sharedConfig": true
  }
}
```

### Використання в Python коді

```python
# В binance_signal.py можна читати результати браузерного бота
import json
import os

def read_browser_results():
    results_path = 'browser-automation/data/last_execution.json'
    if os.path.exists(results_path):
        with open(results_path, 'r') as f:
            return json.load(f)
    return None

# Приклад використання
browser_results = read_browser_results()
if browser_results and browser_results['success']:
    print(f"Браузерний бот успішно виконав конвертацію {browser_results['fromCurrency']} -> {browser_results['toCurrency']}")
```

## 🛠️ Режими роботи

### 1. Демо режим (БЕЗПЕЧНО)
```bash
node start.js
# Оберіть "5. Демо режим"
```
- Без реальних транзакцій
- Тестування всіх функцій
- Перевірка проксі та anti-detection

### 2. Одноразовий запуск
```bash
node start.js
# Оберіть "1. Запустити браузерний бот"
```

### 3. Запланований режим
```bash
node start.js
# Оберіть "2. Запустити заплановано"
```
- Автоматичний запуск через інтервали
- Мінімум 30 хвилин між запусками

### 4. Тестування
```bash
node start.js
# Оберіть "3. Тестування системи"
```

## 🔒 Безпека та рекомендації

### ✅ Обов'язково:
- Використовуйте проксі (residential кращі)
- Почніть з малих сум (10-50 USDT)
- Увімкніть 2FA на Binance
- Регулярно змінюйте проксі

### ❌ Уникайте:
- Запуску без проксі в production
- Надто частих запусків (менше 30 хв)
- Великих сум без тестування
- Комітування .env файлу

### 🔧 Оптимальні налаштування:
```env
HEADLESS=true
MIN_DELAY=3000
MAX_DELAY=7000
RUN_INTERVAL=30
```

## 📊 Моніторинг

### Логи
```bash
# Перегляд логів
tail -f logs/bot.log

# Або через інтерфейс
node start.js
# Оберіть "7. Моніторинг логів"
```

### Результати виконання
Кожне виконання зберігається в:
```
data/last_execution.json
```

### Сповіщення
```env
# Slack webhook
WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK
ENABLE_NOTIFICATIONS=true
```

## 🐳 Docker (опціонально)

```bash
# Збірка
docker build -t binance-browser-bot .

# Запуск
docker run --env-file .env binance-browser-bot
```

## 🆘 Усунення проблем

### Браузер не запускається
```bash
# Перевстановлення браузерів
npx playwright install chromium --force

# Linux залежності
sudo apt install -y chromium-browser
```

### Проксі не працює
```bash
# Тестування проксі
curl --proxy http://user:pass@proxy:port https://httpbin.org/ip
```

### Капча або блокування
- Змініть проксі провайдера
- Збільште затримки (MIN_DELAY, MAX_DELAY)
- Використовуйте residential проксі
- Перевірте User-Agent в коді

### Python інтеграція
```bash
# Перевірка шляхів
node start.js
# Оберіть "4. Перевірка конфігурації"
```

## 🌐 Cloud розгортання

### Render.com
1. Завантажте код на GitHub (БЕЗ .env!)
2. Створіть Web Service на render.com
3. Налаштування:
   ```
   Build Command: npm install && npx playwright install chromium
   Start Command: npm start
   ```
4. Додайте змінні середовища через Render dashboard

### Railway.app
```bash
npm install -g @railway/cli
railway deploy
railway variables set HEADLESS=true
```

## 🔄 Workflow з Python проектом

### Типовий сценарій:
1. **Python сигнали** аналізують ринок
2. **Браузерний бот** виконує транзакції через UI
3. **Результати** зберігаються для Python аналізу
4. **Логи** об'єднуються для моніторингу

### Синхронізація:
- Спільний .env файл
- Об'єднані логи
- JSON обмін даними
- Спільна папка data/

## 📞 Підтримка

Для питань по браузерній автоматизації:
1. Перевірте логи: `logs/bot.log`
2. Запустіть тести: пункт 3 в меню
3. Перевірте конфігурацію: пункт 4 в меню

## ⚠️ Відмова від відповідальності

Цей браузерний бот призначений для освітніх цілей та інтеграції з існуючим CryptoTrade проектом. Користувачі несуть повну відповідальність за дотримання правил Binance та використання в рамках дозволеного.